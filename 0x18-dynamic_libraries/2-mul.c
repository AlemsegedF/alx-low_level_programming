#include "main.h"
/**
 * mul - Entery point
 * Description - multiplies two numbers
 * @a: its a number enterd
 * @b: its a number entered
 * Return: result
 */
int mul(int a, int b)
{
	return (a * b);
}
